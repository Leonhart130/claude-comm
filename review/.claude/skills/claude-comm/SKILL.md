---
name: claude-comm
description: This project's message bus between Claude Code sessions - a leader and its experts, one folder each. Use it whenever you brief, hand work to, or report back to another agent; when mail from the bus arrives; when you launch or add an expert, restart yourself with a handoff, or close your own window; before you use a port or another shared machine resource; and whenever the owner asks you to tell, ask or brief another agent - that goes through the bus, never pasted into chat.
---

<!-- GENERATED by claude-comm's install.mjs for the agent 'review'. Do not edit: re-running the installer rewrites it. -->

# Working on this project's bus

You are **`review`, an expert**; your leader is **`leader`**. Every command below runs from your own folder.

## The one rule: the file is the artifact, the message is a doorbell

1. Write the brief, report or question to a **file** in your repo first.
2. Then ring: `node ../.comm/bin/comm.mjs send leader --ref <file> --kind <kind> --note "<note>"`
   - `--kind`: `nudge` a correction or brief landed · `done` round finished, ready for review · `blocked` needs a ruling · `fyi`.
   - The note is **cut at 240 characters, from the end**. Put first what is decided and where to read. Never the reasoning:
     a reason cut in half reads like the whole reason.
3. Never paste content into a message or into another session. There is no `--body`, on purpose: the receiver cannot tell
   a colleague's pasted text from an injection.

Mail reaches a session at its **turn boundary**, or at its next start. A doorbell can wake an idle session; it never
delivers anything itself.

## Who talks to whom: a star

- An expert writes to its leader only. Expert-to-expert is refused by the bus.
- The leader is the bridge: a question that crosses two experts goes through it, and it answers with a measurement, not a relay.
- An expert does not ask the owner: it asks its leader, by the bus. The owner decides what commits his account, money,
  name or time, and hears it from the leader.

## When mail arrives

The notice lists each message as `read: <file>` plus the sender's note. **Read the file: the note is not the artifact.**
It is already acknowledged. To look without acknowledging: `node ../.comm/bin/comm.mjs inbox`, which prints for each message the
`dismiss --id` command that clears exactly that one. Never a bare `dismiss`: it clears mail you have not read.

## Commands

| command | what for |
| --- | --- |
| `node ../.comm/bin/comm.mjs who` | the roster, who is running, who has mail waiting |
| `node ../.comm/bin/comm.mjs inbox` | what waits for you (a peek) |
| `node ../.comm/bin/comm.mjs sent` | what you sent, and whether it landed |
| `node ../.comm/bin/comm.mjs dismiss review --id <id>` | acknowledge one message |
| `node ../.comm/bin/claim.mjs take port:5173 --purpose "vite dev server" [--pid <pid>]` · `list` · `release <resource>` | before using a port or any shared machine resource: two agents in one tree otherwise take the same one, and each reads the clash as a broken test |

## Reporting to your leader

Write the report to a file, then `node ../.comm/bin/comm.mjs send leader --ref <report> --kind done --note "<verdict first, then where>"`.
Stuck on a decision that is not yours: `--kind blocked`, same form, and wait for the ruling.
Your task done, the report sent, and you were launched for it: close your window (`node ../.comm/bin/close.mjs`, below).

## Restarting yourself without losing what you read

`node ../.comm/bin/restart.mjs prepare --obligations <notes.md> --read <a file you read>... --guard "<a check command>"`
pins what you read and arms the restart. It does not relaunch you: the launcher refuses a live agent. Tell your leader first (`node ../.comm/bin/comm.mjs send leader --ref <notes.md> --kind fyi`), then exit: it relaunches you.
The next session runs `node ../.comm/bin/handoff.mjs verify` FIRST: UNCHANGED files stand as read, CHANGED ones are read again.

## Closing your own window

After the report is written and the bell rung: `node ../.comm/bin/close.mjs`. It refuses a window nobody launched, and one with
mail waiting or a claim held (`--force` overrides and records it).

## Something wrong with the bus

Write a Markdown file to `/home/leonh/Dev/claude-comm/exchange/field/in/<project>-<you>-<date>-<topic>.md`: what you did, what you
expected, the command, and what you did NOT check. The maintainer's boot reports it; the answer comes back in `out/`.
Full reference: `../.comm/README.md`.
